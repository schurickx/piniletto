import React from 'react';

const ArrayColors = () => {
    return (
        <section className="array-colors">
            <h3>Цвета массива</h3>
        </section>
    );
};

export default ArrayColors;